# Task: Channel Sidebar Draw Icon

## Requirements
Update `src/components/ChannelSidebar.tsx`:
- Show a pencil/brush icon for the #draw channel instead of the normal `#` prefix
- Use unicode pencil character or inline SVG

## Dependencies
- step1-schema-and-backend (needs the draw channel seeded)
